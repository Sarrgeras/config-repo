server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sarrgeras/config-repo.git
          default-label: main
          clone-on-start: true
          skip-ssl-validation: true
          timeout: 10